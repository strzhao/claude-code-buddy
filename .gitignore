# Build
.build/
DerivedData/
ClaudeCodeBuddy.app/

# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/

# Sensitive files
.env
.env.*
*.pem
*.key
*.p12
*.cer
credentials.json
secrets.json

# macOS
.DS_Store
*.swp
*~

# Tools
.superpowers/
.claude/worktrees/
.claude/scheduled_tasks.lock
.autopilot/runtime/
# autopilot 活跃任务标记（瞬态状态文件，不入库）
.autopilot/active

# Test artifacts
test-results/

# Sprite processing backup
Sources/ClaudeCodeBuddy/Assets/Sprites/backup/

# qr-gen binary 排除规则已移除（M8 qr shell 化：qr-gen.swift + binary 删除，改 qr-gen.sh 脚本入库）。
# 注：整个 plugins/ 目录仍由 apps/desktop/Sources/.../plugins/.gitignore 的 `*` 规则排除
# （fetch-plugins build-time clone 覆盖填充，不入库）。

# fetch-plugins 缓存（C8 兜底用，构建产物不入库）
apps/desktop/.cache/

claude-code-buddy-web/

# === monorepo additions ===
node_modules/
.next/
dist/
*.tsbuildinfo
.env*
!.env.example
.vercel/
coverage/
playwright-report/
test-results/
.turbo/

# autopilot visual companion 本地配置（devPort 每机器不同，不入库）
local-config.json
